local lp = game.Players.LocalPlayer
local char = lp.Character
function bypassTP(cf, time)
    local hrp = char.PrimaryPart
    local ts = game:GetService('TweenService')
    ts:Create(hrp,
    TweenInfo.new(time, Enum["EasingStyle"].Linear),
    {CFrame = cf
    }):Play()
end

bypassTP(CFrame.new(-1427,43,-1800), 1)

wait(1)

local lp = game.Players.LocalPlayer
local char = lp.Character
function bypassTP(cf, time)
    local hrp = char.PrimaryPart
    local ts = game:GetService('TweenService')
    ts:Create(hrp,
    TweenInfo.new(time, Enum["EasingStyle"].Linear),
    {CFrame = cf
    }):Play()
end


bypassTP(CFrame.new(-1458,43,-1801), 1)

wait(0.7)

local lp = game.Players.LocalPlayer
local char = lp.Character
function bypassTP(cf, time)
    local hrp = char.PrimaryPart
    local ts = game:GetService('TweenService')
    ts:Create(hrp,
    TweenInfo.new(time, Enum["EasingStyle"].Linear),
    {CFrame = cf
    }):Play()
end


bypassTP(CFrame.new(-1471,45,-1748), 1)

wait(1)

local lp = game.Players.LocalPlayer
local char = lp.Character
function bypassTP(cf, time)
    local hrp = char.PrimaryPart
    local ts = game:GetService('TweenService')
    ts:Create(hrp,
    TweenInfo.new(time, Enum["EasingStyle"].Linear),
    {CFrame = cf
    }):Play()
end


bypassTP(CFrame.new(-1510,43,-1748), 1)

wait(3)

local lp = game.Players.LocalPlayer
local char = lp.Character
function bypassTP(cf, time)
    local hrp = char.PrimaryPart
    local ts = game:GetService('TweenService')
    ts:Create(hrp,
    TweenInfo.new(time, Enum["EasingStyle"].Linear),
    {CFrame = cf
    }):Play()
end


bypassTP(CFrame.new(-1511,43,-1904), 1)

wait(3)

local lp = game.Players.LocalPlayer
local char = lp.Character
function bypassTP(cf, time)
    local hrp = char.PrimaryPart
    local ts = game:GetService('TweenService')
    ts:Create(hrp,
    TweenInfo.new(time, Enum["EasingStyle"].Linear),
    {CFrame = cf
    }):Play()
end


bypassTP(CFrame.new(-1495,43,-1917), 1)

wait(1.5)



local lp = game.Players.LocalPlayer
local char = lp.Character
function bypassTP(cf, time)
    local hrp = char.PrimaryPart
    local ts = game:GetService('TweenService')
    ts:Create(hrp,
    TweenInfo.new(time, Enum["EasingStyle"].Linear),
    {CFrame = cf
    }):Play()
end


bypassTP(CFrame.new(-1165,43,-1917), 2)

wait(2)

local lp = game.Players.LocalPlayer
local char = lp.Character
function bypassTP(cf, time)
    local hrp = char.PrimaryPart
    local ts = game:GetService('TweenService')
    ts:Create(hrp,
    TweenInfo.new(time, Enum["EasingStyle"].Linear),
    {CFrame = cf
    }):Play()
end


bypassTP(CFrame.new(-1147,43,-1900), 1)

wait(2)

local lp = game.Players.LocalPlayer
local char = lp.Character
function bypassTP(cf, time)
    local hrp = char.PrimaryPart
    local ts = game:GetService('TweenService')
    ts:Create(hrp,
    TweenInfo.new(time, Enum["EasingStyle"].Linear),
    {CFrame = cf
    }):Play()
end


bypassTP(CFrame.new(-1147,43,-1662), 2)

wait(3)

local lp = game.Players.LocalPlayer
local char = lp.Character
function bypassTP(cf, time)
    local hrp = char.PrimaryPart
    local ts = game:GetService('TweenService')
    ts:Create(hrp,
    TweenInfo.new(time, Enum["EasingStyle"].Linear),
    {CFrame = cf
    }):Play()
end


bypassTP(CFrame.new(-1061,44,-1643), 0.5)

wait(0.5)

local lp = game.Players.LocalPlayer
local char = lp.Character
function bypassTP(cf, time)
    local hrp = char.PrimaryPart
    local ts = game:GetService('TweenService')
    ts:Create(hrp,
    TweenInfo.new(time, Enum["EasingStyle"].Linear),
    {CFrame = cf
    }):Play()
end


bypassTP(CFrame.new(-1056,44,-1560), 0.5)

wait(0.5)

local lp = game.Players.LocalPlayer
local char = lp.Character
function bypassTP(cf, time)
    local hrp = char.PrimaryPart
    local ts = game:GetService('TweenService')
    ts:Create(hrp,
    TweenInfo.new(time, Enum["EasingStyle"].Linear),
    {CFrame = cf
    }):Play()
end


bypassTP(CFrame.new(-956,19,-1482), 0.5)
